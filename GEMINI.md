# قواعد صلاحيات المستخدمين (User Roles & Permissions)

هذه القواعد تُحدد صلاحيات المستخدمين في نظام `Kody ERP` ويجب الالتزام بها عند برمجة أي شاشة أو وحدة (Module) وتطبيق الـ Middleware الخاص بالصلاحيات عليها.

## 1. هيكل الصلاحيات (Permission Structure)

نعتمد في إدارة الصلاحيات على حزمة **`spatie/laravel-permission`**، وتقسم منظومة الصلاحيات إلى 3 أقسام رئيسية:

### أ. صلاحيات القوائم (Menu & CRUD Permissions)
عبارة عن مصفوفة (Matrix) تتحكم في الشاشات بناءً على العمليات الأساسية الأربع (CRUD) لكل قائمة أو وحدة (Module):
- **عرض (Read / View)**
- **جديد (Create / Add)**
- **تعديل (Update / Edit)**
- **حذف (Delete)**
*قاعدة (Rule): عند برمجة أي Controller أو واجهة، يجب التحقق من هذه الصلاحيات الأربع بشكل صريح.*

### ب. صلاحيات المستخدمين (User Permissions / Scopes)
صلاحيات متقدمة تتعلق ببيئات العمل أو صلاحيات الاستثناء، مثل:
- إمكانية تعديل سعر البيع أثناء الفاتورة.
- إمكانية عمل خصم (تجاوز الحد المسموح).
- إمكانية رؤية فواتير البائعين الآخرين (لمدير المبيعات).
- إمكانية البيع بأقل من سعر التكلفة.

### ج. افتراضيات المستخدم (User Defaults)
إعدادات يتم ربطها بالمستخدم لتسريع عمله ومنع تداخل البيانات، وتشمل:
- **المخزن الافتراضي:** (المخزن الذي يتم الخصم منه تلقائياً عند بيع هذا المستخدم).
- **الصندوق الافتراضي:** (الخزينة التي تصب فيها أموال الكاشير).
- **العميل الافتراضي:** (عميل "نقدي" لسرعة إصدار فواتير POS).

## 2. قواعد التطبيق الإلزامية (Mandatory Application Rules)
- **يجب** استخدام حزمة `spatie/laravel-permission` لتطبيق الصلاحيات أعلاه (أ و ب).
- **يجب** تمرير "صلاحيات المستخدم الحالي" إلى الـ Frontend (Vue/Inertia) لإخفاء/إظهار الأزرار (مثل زر 'حذف' أو 'إضافة') بناءً على مصفوفة "صلاحيات القوائم".
- **يجب** أن يحتوي الـ Backend (الـ Controllers) على تحقق صارم (Middleware أو Gates) لكل عملية CRUD لضمان الحماية الكاملة للمسارات (Routes).

## 3. قواعد واجهة المستخدم العامة (Global UI/UX Rules)
- **اختصارات لوحة المفاتيح (Keyboard Shortcuts):** يجب إضافة اختصارات لوحة المفاتيح في جميع الشاشات لتسريع العمل (مثل الحفظ، الإضافة، التراجع).
- **القوائم المنسدلة (Searchable Selects):** جميع القوائم المنسدلة يجب أن تكون قابلة للبحث (Searchable)، تجنب استخدام `<select>` العادي واستخدم مكون يدعم البحث.
- **ترتيب التنقل (Tab Orders & Keyboard Navigation):** يجب أن يكون التنقل بين الحقول مدعوماً بشكل كامل وسلس باستخدام أزرار `Enter` و `Arrows` و `Tab` بترتيب منطقي.